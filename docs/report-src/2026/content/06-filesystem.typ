= 文件系统

== 设备抽象层

F7LY 的设备层目标是把“硬件差异”和“文件接口”隔离开。字符设备和传统块设备通过 `VirtualDevice` / `DeviceManager` 暴露设备类型、中断处理和读写就绪状态；virtio-blk 走独立的跨架构队列框架；loop 设备由 `/dev/loop-control` 管理实例，并由 `/dev/loopN` 虚拟设备节点转发到后端文件。它们对上最终都收束为 VFS fd 或设备节点，使 VFS、epoll 和系统调用层主要关心 fd 是否可读写。

字符设备面向字节流，承担控制台、串口、伪终端等交互式 I/O。流设备在字符设备之上补齐批量读写能力，使 VFS 可以用普通文件的方式处理终端输入输出。传统块设备面向固定大小的数据块，服务 ramdisk 等内存块后端，并支持一次请求携带多个缓冲区；virtio-blk 的批量传输由独立队列层承担。

Loop 设备把普通文件包装成可被块语义访问的对象。控制面通过 `/dev/loop-control` 创建和管理实例，数据面通过 `/dev/loopN` 的虚拟设备节点把块读写映射到后端文件的对应偏移。这样文件系统镜像可以像真实磁盘分区一样被挂载，同时后端文件的生命周期由 loop 设备独立持有，不受原始 fd 关闭影响。

== 块设备驱动与 I/O 调度

=== 统一 virtio-blk 框架

F7LY 的 virtio-blk 驱动采用三层框架，将硬件传输、请求队列和块设备接口拆开。RISC-V 使用 MMIO，LoongArch 使用 PCI，但这种差异只存在于传输层；队列层和设备层共享同一套请求管理逻辑。

#figure(
  image("fig/块设备驱动与IO调度.png", width: 100%),
  caption: [块设备驱动与IO调度],
) <fig:fs-virtio-blk-architecture>

传输层负责 DMA 地址转换、队列通知和中断确认。队列层负责描述符分配、请求排序、完成回收和并发控制。设备层向上提供同步提交接口，把 buffer cache、ext4、FAT32、loop 等来源的请求统一转换成扇区读写。请求进入队列时携带提交进程和优先级信息，使 CPU 调度和磁盘调度共享同一套优先级语义。

=== mClock 调度器

mClock 调度器位于请求进入 virtqueue 之前，用于决定多个进程并发 I/O 的派发顺序。F7LY 将进程 nice 值映射到若干服务类，每个服务类内部再按进程维护独立 flow，避免单个进程独占同一优先级的磁盘带宽。

$ R_i^r = max{ R_i^{r-1} + 1/r_i, t } $

$ L_i^r = max{ L_i^{r-1} + 1/l_i, t } $

$ P_i^r = max{ P_i^{r-1} + 1/w_i, t } $

调度器同时考虑三类约束：保底带宽保证低频请求不会长期饥饿，权重标签决定空闲带宽如何分配，上限标签为后续限速预留接口。磁盘实际吞吐会通过完成路径动态估计，调度器据此调整标签推进速度。只有一个活跃类别时，调度器直接走快速轮转路径，减少不必要的扫描。

#figure(
  image("fig/mClock.png", width: 100%),
  caption: [Reservation标签调整],
) <fig:fs-mclock-scheduler>

== VFS 与文件对象

VFS 在 F7LY 中承上启下。对上，文件相关系统调用最终都落到统一的 `file` 对象；对下，VFS 分发到 ext4、FAT32、虚拟文件、设备文件、管道、socket 和 epoll 等不同后端。用户态看到的是同一套 fd 语义，内核内部则由多态文件对象承接差异。

#figure(
  image("fig/vfs_structure.png", width: 100%),
  caption: [vfs_structure],
) <fig:fs-vfs-structure>

路径打开时，VFS 先判断是否命中内存中的虚拟文件树，其次识别设备路径，剩余路径进入磁盘文件系统解析。解析成功后，内核根据资源类型创建对应文件对象，并把它放入进程 fd 表。读写、seek、目录遍历、就绪判断等操作都通过文件对象转发到底层后端。

F7LY 的文件对象体系可以概括为七类：普通文件和目录对接 ext4/FAT32；设备文件和虚拟设备节点对接设备管理器或虚拟文件 provider；管道文件对接内核循环缓冲区；socket 文件对接网络数据面；虚拟文件按需生成内核信息；epoll 文件维护关注 fd 集合并统一查询就绪状态。

== VFS 核心元数据

=== Buffer 层

Buffer 层位于文件系统和磁盘驱动之间，负责缓存磁盘块、合并写回并提供块级同步点。ext4 主要通过 lwext4 的块缓存访问磁盘，缓存未命中时再下探到底层 buffer 和 virtio-blk。FAT32 也使用同一块设备通路，因此两套文件系统最终共享相同的磁盘队列和调度策略。

为提高连续块读写和高频小写入的吞吐，F7LY 在两处做了聚合。其一，ext4 向下提交相邻逻辑块时，块设备适配层会把连续块合并成更大的 DMA 传输，减少 virtqueue 描述符分配、队列通知和中断回收次数。其二，普通文件写路径在进入 ext4 前设置 write-combine 缓冲区，把密集小写入合并后再提交，降低文件系统事务和锁竞争开销。

=== SuperBlock、Inode 与 File

VFS 元数据分为三层，每层只负责自己必须表达的抽象。

- *SuperBlock* 表示一个已挂载的持久化文件系统，保存块大小、文件系统类型、根 inode 和脏元数据回写入口。ext4 的磁盘布局细节仍由 lwext4 的超级块结构维护，VFS 只抽取跨文件系统通用的信息。虚拟文件系统不使用超级块，它直接由内存目录树描述。
- *Inode* 表示文件系统中的对象，记录文件类型、权限、大小、时间戳和底层文件系统私有信息。ext4 通过 extent 和目录索引定位数据，FAT32 通过簇链定位数据，但 VFS 上层只通过 inode 操作表访问。
- *File* 表示一次打开后的运行时状态，保存文件偏移、打开标志、引用关系和后端对象。两个 fd 可以指向同一个 inode，但拥有不同的 file 状态；pipe、socket、epoll 等非磁盘对象也通过 file 接入同一 fd 体系。

这三层共同贡献了 VFS 的主要抽象边界：superblock 管文件系统实例，inode 管目录树对象，file 管打开后的会话状态。系统调用层不直接操作 ext4 或 FAT32 的内部结构，而是经由这三层完成分发。

== ext4 与 FAT32

F7LY 同时支持 ext4 与 FAT32。ext4 是主力根文件系统，承载系统目录、用户程序、配置文件和动态链接器；FAT32 作为辅助数据盘，服务数据交换和兼容场景。两套文件系统在磁盘布局和目录查找方式上差异很大，但在 VFS 之上暴露相同的文件对象和 inode 操作。

ext4 通过 lwext4 提供 extent、日志、目录索引和扩展属性支持；FAT32 使用内核内置实现维护 BPB、FSINFO 和簇链。二者的块读写最终都收口到统一 virtio-blk 框架，因此底层磁盘驱动不感知请求来自哪一种文件系统。

== 文件操作

除基本读写外，F7LY 在文件描述符层提供若干高级能力：

- `fcntl` 管理 fd 属性、非阻塞状态、文件锁、管道容量和 memfd 密封。
- `ioctl` 作为设备控制通道，按块设备、终端、socket 等类型拆分处理。
- 扩展属性为文件提供独立于正文数据的键值存储。
- `splice`、`sendfile` 和 `copy_file_range` 在内核内搬运数据，减少用户态往返拷贝。
- `fanotify` 和 inotify 为文件访问、修改和目录事件提供通知入口。

这些能力都以 fd 为统一入口，真正的差异由对应文件对象或设备后端处理。

== 挂载与路径解析

挂载系统负责把不同设备和文件系统接入统一目录树。F7LY 支持常规挂载、bind 挂载和挂载点移动，并维护私有、共享、从属等传播属性。每个进程属于一个 mount namespace，namespace 内的挂载变更不会直接影响其他 namespace，最后一个持有者退出后对应视图自动释放。

路径解析先完成语法规范化，再沿当前 mount namespace 的目录树逐级解析。解析过程中会区分虚拟路径、设备路径和磁盘路径，并处理符号链接、相对目录 fd、根目录限制等语义。最终返回的不是底层文件系统私有对象，而是 VFS 可操作的 inode 或 file。

== 虚拟文件与特殊设备

F7LY 的 VFS 还维护一组不依赖磁盘的虚拟文件和特殊设备。它们在用户态表现为普通路径，但数据来自内核状态、配置表、硬件设备或另一个文件对象。正文只按目录说明职责，具体节点由初始化注册表集中维护。

- `/proc` 是进程与系统运行状态窗口，提供进程自身信息、内存映射、挂载视图、内存统计、CPU 信息、中断统计和 System V IPC 状态等动态内容。
- `/proc/sys` 是可读写内核参数视图，用于暴露进程号上限、共享内存限制、管道限制、随机熵、网络标签等可配置状态。
- `/sys` 提供 CPU 和 NUMA 拓扑信息，使用户态工具可以查询在线 CPU、可用节点和节点距离等硬件视图。
- `/etc` 由内核内置配置文件补齐基础运行环境，例如用户组、主机名解析、动态链接器、DNS、协议号和时区信息。
- `/boot` 暴露内核配置文件节点，供用户态工具读取当前内核能力。
- `/dev` 注册字符设备、块设备、loop 设备、伪终端、随机数设备、实时时钟和网络隧道等特殊入口。

这些路径的共同点是：它们都复用 VFS 的 `open`、`read`、`write` 和 `close` 语义，但数据来源不一定是磁盘。VFS 因此成为普通文件、内核状态和设备控制之间的统一边界。
