= 进程与线程管理

== 进程控制块（PCB）

进程控制块（Process Control Block，PCB）是内核管理进程与线程的核心数据结构。F7LY 以 `Pcb` 统一描述一个任务的全部内核态状态，将标识、调度、地址空间、文件系统、信号处理、同步原语、资源限制和时间统计等信息收拢到同一个对象中。


进程与线程在 F7LY 中统一用 `Pcb` 表示，区别在于资源共享关系：线程共享地址空间和线程组，但仍拥有独立的内核栈、陷阱帧和线程 ID。内核通过线程 ID 与线程组 ID 的关系区分主线程和普通线程。以下两小节分别展开 PCB 的组成和状态机。

#figure(
  image("fig/PCB结构.png", width: 100%),
  caption: [PCB结构示意图],
) <fig:proc-pcb-structure>


=== 组成

```cpp
class Pcb;
extern Pcb k_proc_pool[num_process];
```

`Pcb` 是进程控制块的类型名，`k_proc_pool` 是全局进程池，以静态数组形式容纳系统中所有进程与线程的 PCB。每个 `Pcb` 对象承载一个任务的完整上下文，详细字段放于文末附录进行介绍。

=== 状态机

进程在其生命周期中会处于不同的状态。F7LY 使用七种状态描述任务从空闲、分配、就绪、运行、阻塞、停止到退出待回收的完整过程。

#figure(
  table(
    columns: (2cm, 12cm),
    align: (left, left),
    table.header(
      [*状态*], [*含义*]
    ),
    [`UNUSED`], [进程池槽位空闲，PCB 尚未被分配。],
    [`USED`], [PCB 已被分配但尚未就绪调度，正在初始化运行所需的资源。],
    [`RUNNABLE`], [进程准备就绪，等待调度器分配 CPU。],
    [`RUNNING`], [进程正在 CPU 上执行。],
    [`SLEEPING`], [进程因等待某个资源或事件而阻塞，条件满足后被唤醒回到就绪状态。],
    [`STOPPED`], [进程被 job-control 停止信号暂停，收到继续信号后恢复就绪。],
    [`ZOMBIE`], [进程已终止，但 PCB 保留退出状态等待父进程回收。],
  ),
  caption: [进程七状态说明],
) <tab:proc-state-overview>

进程状态的切换如下：

- 进程池创建时，所有 PCB 初始化为 `UNUSED`。
- 创建新进程时，从池中取出空闲 PCB，分配 PID、trapframe 和内核栈后，切换为 `USED`。
- 初始化完成、具备运行条件后，切换为 `RUNNABLE`，进入调度器的就绪队列。
- 调度器选中该进程后，状态变为 `RUNNING`，进程开始执行。
- 时间片耗尽或进程主动让出 CPU 时，状态回到 `RUNNABLE`。
- 进程等待资源或事件时（如管道读写、futex 等待），状态变为 `SLEEPING`；条件满足后被唤醒，回到 `RUNNABLE`。
- 进程收到停止信号（`SIGSTOP`、`SIGTSTP` 等）时，状态变为 `STOPPED`；收到 `SIGCONT` 后恢复为 `RUNNABLE`。
- 独立进程或线程组主线程退出时，状态变为 `ZOMBIE`，保留退出码等待父进程回收；普通线程完成清理后直接归还 PCB。

== 进程调度

调度器负责决定下一个在 CPU 上运行的任务。F7LY 的调度建立在三个机制之上：上下文切换完成执行现场保存与恢复，轮转策略保证就绪任务公平获得 CPU 时间，优先级策略让高优先级任务更早被选中。以下三小节依次展开。

=== 上下文切换

上下文切换是调度器在进程之间切换 CPU 的底层机制。当一个进程让出 CPU、另一个进程被选中时，内核需要保存当前进程的执行现场，并恢复目标进程之前保存的现场——这样目标进程从上次停下的位置继续执行，就像从未离开 CPU。

保存和恢复的现场由内核态上下文记录，主要包括栈指针、返回地址以及被调用者保存寄存器。实际切换由架构汇编完成：当前任务切出时保存自己的上下文并回到调度器，目标任务切入时从已保存的上下文继续执行。

一次完整调度由“任务切出到调度器”和“调度器切入另一个任务”两个动作组成。两次切换形成镜像，使每个任务再次被调度时都能从上一次停下的位置继续执行。

=== 轮转调度

轮转调度是调度器的基本策略：调度循环按进程池顺序扫描可运行任务，运行任务在时间片耗尽或主动让出时回到就绪态，从而让就绪任务持续获得执行机会。

调度循环每轮先计算当前最高有效优先级，再从进程池中选择状态为 `RUNNABLE` 且优先级匹配的任务执行。任务被选中后状态切为运行态；当它因为时间片耗尽、主动让出或等待资源而切出时，调度器继续寻找下一个就绪任务。

时间片的计量依赖时钟中断。每次 timer tick 到来时，中断处理程序检查当前进程是否已用完本轮时间片；若已用完，则触发 `yield` 将进程状态从 `RUNNING` 改回 `RUNNABLE`，并调用 `swtch` 切回调度器，让下一个进程上场。

=== 优先级调度

在轮转调度的基础上，调度器引入优先级机制，使高优先级进程优先获得 CPU。

每个进程有一个优先级数值（nice 值，范围 `[-20, 19]`，越小优先级越高），默认值为 `0`。nice 值不仅影响 CPU 调度，也同时影响块层 I/O 的排队顺序——计算密集型和 I/O 密集型的优先级策略在此统一。进程还可以单独设置 I/O 优先级覆盖值，在保持 CPU 调度不变的前提下调整磁盘访问的优先顺序。

调度器在每轮扫描进程池时，先遍历所有进程找出当前就绪进程中的最高优先级（即最小 nice 值），然后以该数值作为门槛：只选择优先级等于该值的进程执行。这保证了只要存在高优先级的就绪进程，低优先级进程就不会被选中。当所有进程的优先级都是默认值 `0` 时，调度器通过一个布尔标记跳过完整的优先级统计，直接退化为纯轮转——这是最常见的场景，几乎不引入额外开销。

此外，调度器在计算优先级时会将信号状态纳入考虑：当进程有未屏蔽的信号等待处理时，该进程在当轮调度中会被临时视为最高优先级（`-20`）。这确保信号处理函数能尽快在用户态执行，而不必排在与它同优先级的其他进程之后。这种提升只影响调度器内部的当轮决策，不改变进程本身的 nice 值。

== 进程生命周期

进程的生命周期涵盖创建、执行与退出三个阶段。F7LY 通过 `fork` / `clone` 创建新任务，通过 `execve` 装载新程序映像，通过 `exit` / `wait` 终止与回收。此外，`clone` 支持创建共享地址空间的线程，使多线程编程成为可能。以下四小节依次展开。

=== 创建（fork / clone / clone3）

新任务的创建由 `fork` 和 `clone` 两个系统调用完成。`fork` 是传统的进程复制语义——创建一个与父进程几乎完全相同的子进程。`clone` 在此基础上增加了标志位控制，允许调用者精细决定哪些资源在父子间共享、哪些独立复制，从而既能创建进程，也能创建线程。`clone3` 是 `clone` 的扩展版本，提供了更大的参数空间和更整洁的接口。

创建过程的核心步骤相同：首先从全局进程池中分配一个空闲 PCB，为其分配独立的 PID 和 TID，创建专用的 trapframe 和内核栈；然后根据调用类型决定地址空间、文件描述符表和信号处理表的处理方式——`fork` 将这些资源深拷贝给子进程，`clone` 则根据标志位选择性地共享。

子进程首次被调度时，从其内核栈上恢复上下文，并从 `fork` 的返回点开始执行。`fork` 的返回值是区分父子进程的关键：父进程收到子进程的 PID，子进程收到 `0`。`clone` 在线程模式下返回新线程的 TID，在进程模式下返回新进程的 PID。

`clone` 的关键标志位决定父子之间共享哪些资源。线程创建通常共享地址空间、文件描述符表和信号处理表，并加入同一个线程组；进程创建则保留独立资源。另一些标志负责 vfork 阻塞、线程局部存储以及线程退出时的清零和唤醒语义，为用户态线程库提供底层支撑。

=== 装载（execve）

`execve` 的功能是用一个新的程序映像替换当前进程的地址空间。与 `fork` 不同，它不创建新进程，而是在当前进程中销毁旧的代码、数据和栈，加载新的可执行文件并从头开始执行。进程的 PID、文件描述符表（除标记了 `CLOEXEC` 的项外）和信号屏蔽字等属性保持不变。

装载过程的核心是解析 ELF 可执行文件。内核读取 ELF 文件头，遍历程序头表中的 `PT_LOAD` 段，将每个段加载到指定的虚拟地址，并按其声明的权限（读、写、执行）进行映射。如果 ELF 包含 `PT_INTERP` 头——即程序是动态链接的——内核还需额外加载动态链接器（如 `/lib/ld-musl-riscv64-sf.so.1`）的各 LOAD 段。最终的程序入口点设为可执行文件自身的入口地址，或者动态链接器的入口地址。

对于以 `#!` 开头的脚本文件，内核会识别 shebang 行，提取解释器路径和可选参数，递归地以解释器为目标重新启动装载流程。这意味着用户执行脚本时，实际装载的是脚本解释器，脚本路径作为参数传入。

新地址空间构建完成后，内核构造用户栈，依次压入参数和环境变量的字符串、指针数组、以及辅助向量（auxiliary vector），然后原子地替换当前进程的页表，释放旧地址空间。原来注册的信号处理函数中，除设置为忽略（`SIG_IGN`）的以外全部重置为默认行为，信号备用栈也一并清除。最后，将程序计数器设为 ELF 入口地址，栈指针指向新构造的用户栈顶，进程从新程序的第一条指令开始执行。

=== 退出（exit / wait）

进程的退出有两种粒度：`exit` 仅终止当前线程，`exit_group` 终止整个线程组（即进程的所有线程）。无论哪种方式，最终都进入 `exit_proc` 执行清理。

清理过程分两个阶段。首先完成所有必须在地址空间还存活时进行的用户态可见操作：将 `CLONE_CHILD_CLEARTID` 指定的地址清零并执行 futex 唤醒（使 `pthread_join` 得以返回），遍历健壮 futex 链表对持有的互斥锁标记"持有者已死"并唤醒等待者。然后逐步释放进程持有的内核资源——关闭所有文件描述符、释放信号处理表、销毁地址空间（包括 VMA、堆内存和页表）。

资源释放完成后，进入状态终结。普通线程直接归还 PCB，不产生僵尸态；主线程退出时代表整个进程终止，需要保留退出状态、通知父进程，并把遗留子进程交给 init 接管。

父进程通过 `wait4` 或 `waitid` 等待子进程状态变更。当子进程退出变为 ZOMBIE 时，内核将子进程的退出码和运行时间信息返回给父进程，然后回收 PCB。如果父进程明确将 `SIGCHLD` 设置为忽略，或设置了 `SA_NOCLDWAIT` 标志，则子进程退出时不再保留 ZOMBIE，直接自动回收。

=== 线程

线程是同一进程内的独立执行流。F7LY 中线程与进程统一使用 `Pcb` 表示，区别在于 `clone` 时传入的标志位组合。线程的完整生命周期如下：

*创建*——用户态线程库（如 musl 的 `pthread_create`）调用 `clone` 系统调用，传入 `CLONE_VM | CLONE_THREAD | CLONE_FILES | CLONE_SIGHAND` 等标志。内核分配新的 PCB、PID 和 TID，但共享父进程的地址空间（`_memory_manager` 引用计数加一）、文件描述符表和信号处理表。新线程拥有独立的内核栈和 TrapFrame。`CLONE_SETTLS` 将线程局部存储的基址写入 `tp` 寄存器，`CLONE_CHILD_CLEARTID` 记录一个用户态地址供退出时使用。

*标识*——新线程继承父进程的线程组标识，同时获得独立线程号。主线程和普通线程的区分由这两个标识之间的关系决定。

*正常退出*——线程调用 `exit` 退出时，先清理用户态线程库约定的退出地址并唤醒等待者，使 join 方能够返回。内核随后释放线程持有的资源；普通线程直接回收 PCB，不产生僵尸态。

*异常退出*——如果线程异常终止（如被信号杀死），内核在清理时遍历线程注册的健壮 futex 链表。对链表中每个被该线程持有的互斥锁，将其标记为 `FUTEX_OWNER_DIED`（持有者已死），并唤醒在该锁上等待的其他线程。这防止了线程异常终止导致的死锁——等待者可以检测到锁的异常状态并采取恢复措施。

*主线程退出*——主线程退出时会终止整个线程组。内核标记同组线程结束，各线程在再次进入内核或被调度时完成清理；主线程自身进入僵尸态，等待父进程回收。
