= 进程间通信

进程间通信（Inter-Process Communication，IPC）是操作系统向用户态程序提供的跨进程数据交换与事件通知机制。F7LY-OS 的 IPC 体系按照两条线组织：信号、POSIX Timer、eventfd 和 epoll 解决事件通知；futex、管道、FIFO、共享内存和 memfd 解决同步与数据交换。

== 信号与定时器

信号是异步事件通知机制。F7LY 的信号子系统围绕四类状态运转：每个任务的待处理信号集合、信号屏蔽字、信号处理动作表，以及进入用户态 handler 前保存的信号帧。只有这几类状态会影响信号语义，具体函数如何拆分不在正文展开。

信号处理路径可以概括为三个阶段。首先，内核或用户进程产生信号，将目标任务的待处理集合置位，必要时唤醒正在等待信号的任务。其次，任务从内核态返回用户态前，内核筛选未被屏蔽的信号，并根据注册动作决定处理方式。若用户注册了 handler，内核在用户栈或备用信号栈上保存被打断现场，改写返回上下文，使任务回到用户态后先执行 handler；若信号采用默认动作，则内核直接执行终止、停止或忽略。最后，handler 通过 `rt_sigreturn` 返回内核，内核恢复原始寄存器和信号掩码，用户程序继续从被打断的位置运行。

默认动作按 POSIX 语义处理：停止类信号使任务进入暂停状态，继续信号恢复调度，终止类信号进入退出路径，忽略类信号只从待处理集合中清除。实时信号和带数据的信号在 pending 位之外保留一份附加信息，使用户态能够读取发送方和携带值。

POSIX Timer 与 interval timer 都以信号作为到期通知。Interval timer 嵌在进程状态中，分别描述真实时间、用户态 CPU 时间以及用户态加内核态 CPU 时间。POSIX Timer 由全局池管理，支持用户指定时钟类型、通知目标和信号编号。每次时钟中断推进时间后，内核检查到期项，把对应信号加入目标任务的待处理集合；后续处理仍复用统一的信号返回路径。

== futex

futex 是 Linux 提供的一种轻量级同步原语。它的核心思想是：将互斥锁的大部分操作放在用户态完成——线程在无竞争时仅通过一次原子比较交换就能获取或释放锁，完全不需要陷入内核。只有在线程需要等待（锁已被别人持有）或需要唤醒等待者时，才通过 `futex` 系统调用进入内核。这种设计使得 futex 在低竞争场景下几乎等同于纯用户态的原子操作，同时又能处理复杂的阻塞与唤醒逻辑。

F7LY 实现了 futex 的核心语义，包括 `FUTEX_WAIT` 与 `FUTEX_WAKE` 的基本 wait/wakeup、健壮链表（robust list）的退出自动解锁，以及 `FUTEX_REQUEUE` 的等待者迁移。

=== wait 与 wakeup

#figure(
  image("fig/futex.png", width: 70%),
  caption: [futex 工作机制],
) <fig:ipc-futex-mechanism>

`WAIT` 的核心流程是先重新读取用户态地址的值，并与调用者期望值比较。如果不一致，说明状态已在进入内核前变化，内核立即返回，让用户态重新尝试；如果一致，内核才把线程放入等待队列并睡眠。这个二次比较保证了“检查条件”和“进入睡眠”之间不会丢失唤醒。

futex 的等待键使用用户地址对应的物理页身份，而不是裸虚拟地址。这样多个进程通过共享映射看到不同虚拟地址时，仍能落到同一个等待队列。`WAKE` 解析出同一把键后，唤醒指定数量的等待者；被唤醒线程回到用户态后重新执行原子获取逻辑。

等待过程支持相对/绝对超时，也支持被未屏蔽信号打断。超时返回超时错误，信号打断返回中断错误，用户态线程库据此决定重试、返回或进入信号处理。

=== Robust List 与 Requeue

Robust list 用来处理“线程持锁退出”的情况。线程库在持有健壮互斥锁时把锁登记到自己的 robust list，释放时摘除。线程退出时，内核遍历该链表，把仍由它持有的锁标记为“持有者已死亡”，并唤醒等待者。等待者看到该标记后可以修复互斥锁状态，避免永久死锁。

`FUTEX_REQUEUE` 面向条件变量场景。它将“唤醒少量等待者”和“把剩余等待者迁移到另一把锁”合并为一个原子步骤，避免广播唤醒后大量线程争抢同一互斥锁又重新睡眠。F7LY 在 futex 等待队列层完成迁移，保持条件变量和互斥锁交接的一致性。

== 管道与 FIFO

管道是以内核缓冲区中转的字节流。F7LY 将管道端点实现为 VFS 文件对象，因此匿名管道、命名管道和普通 fd 读写都复用同一套打开、关闭、阻塞和非阻塞语义。

=== 管道语义

一个管道由共享循环缓冲区和两类端点组成：读端从缓冲区取出字节，写端向缓冲区追加字节。缓冲区状态、读写端是否仍打开、等待读者和等待写者数量共同决定读写行为。

写入时，如果缓冲区仍有空间，数据进入缓冲区并唤醒读端等待者；如果缓冲区已满，非阻塞写返回重试错误，阻塞写则睡眠等待空间。读出时，如果缓冲区有数据，读端复制并唤醒写端；如果没有数据，写端仍打开时读端阻塞，写端全部关闭时读端返回 EOF。读端全部关闭后，写端收到管道破裂语义并返回错误。

=== FIFO 管理器

匿名管道依靠继承 fd 传递端点，命名管道则以路径作为共享身份。F7LY 的 FIFO 管理器维护“路径到管道对象”的映射，并记录当前读者和写者数量。首次打开某个 FIFO 路径时创建底层管道，后续打开同一路径复用该管道；当读写两端引用都归零时，管理器释放映射和缓冲区。

#figure(
  image("fig/fifomanager.png", width: 80%),
  caption: [fifomanager],
) <fig:ipc-fifo-manager>

打开 FIFO 时，路径映射和端点计数共同决定打开结果。当前实现重点覆盖非阻塞只写且没有读者时返回 `ENXIO`、`O_RDWR` 同时保留读写端、以及后续读写阶段的阻塞/非阻塞语义。路径映射和引用计数由锁保护，避免并发打开/关闭破坏同一个 FIFO 的共享身份。

== 共享内存与 memfd

共享内存允许多个进程把同一批物理页映射到各自地址空间，数据读写不再经过内核中转。F7LY 同时支持 SysV 共享内存和 memfd：前者以全局 key 查找共享段，后者以文件描述符传递共享对象。

=== SysV 共享内存

SysV 共享内存的思路是：内核维护一组命名的共享内存段，进程通过一个整数 key 找到目标段，将它映射到自己的地址空间，之后直接通过指针读写，数据全程走物理内存，内核不参与搬运。

```cpp
  struct shm_segment
  {
      int shmid;              
      key_t key;             
      size_t size;            
      size_t real_size;       
      u16 shmflg;            
      eastl::vector<attached_entry> attached_addrs;  
      SysvShmVmObject *object;                       
      int nattch;             
      bool auto_destroy_on_last_detach;  
      uid_t owner_uid;        
      gid_t owner_gid;
      pid_t creator_pid;      
      time_t atime, dtime, ctime;  
      // ……
  };
```

进程调用 `shmget`，传入一个 key，不同进程用同一个 key 就能找到同一个段。如果该 key 的段已经存在，直接返回它的 ID；如果不存在，需要在 flags 里带上 `IPC_CREAT` 才会现场创建一个。

每个段记录了自己的大小、访问权限（谁可读、谁可写），以及当前有哪些进程把它映射到了哪个虚拟地址。物理页不直接挂在段上，而是由一个专门的 `SysvShmVmObject` 对象管理，页面的分配和回收都走这个对象。这套 VmObject 体系也同时服务于 `mmap MAP_SHARED` 的文件映射，两种共享方式在物理页管理层面上是统一的。

`shmat` 将段附加到进程地址空间，返回一个可直接访问的虚拟地址；`shmdt` 解除附加并减少引用。`shmctl IPC_RMID` 只设置待删除标记：若仍有附加者，段会保留到 `nattch` 归零后销毁；若没有附加者，则立即销毁。没有打删除标记的普通 SysV 段即使暂时无人附加也继续存在；`auto_destroy_on_last_detach` 只用于内核借用同一后端对象的自动清理场景。fork 会继承已有附加关系，进程退出时也会自动解除自己持有的附加。

=== memfd

memfd 是基于 fd 的匿名共享内存。创建方得到一个匿名文件 fd 后，通过共享映射写入数据，也可以经 Unix domain socket 把 fd 传给其他进程。生命周期由 fd 引用和映射引用自然管理，不需要全局 key。

F7LY 的 memfd 借助 VFS 临时文件能力承载数据，同时为 memfd 维护一份共享状态：文件大小、密封位和是否允许继续添加密封。密封一旦设置不可撤销，并在所有修改路径上强制生效。例如写密封会拒绝新的可写共享映射和直接写入，缩小/增长密封会限制截断和预分配。这样 memfd 可以作为安全的只读快照或受限共享缓冲区使用。

== 就绪通知

多路复用与事件通知把“多个 fd 的状态变化”统一成可等待事件。F7LY 实现了 epoll 和 eventfd，并让它们都接入 VFS 文件对象体系。

=== epoll

epoll 将“注册关注 fd”和“等待就绪”分离。关注列表变化时通过控制接口更新，等待时内核只检查已注册的条目，避免每次从用户态重新传入完整 fd 集合。每个关注条目保存目标 fd、事件掩码、用户数据以及边沿触发和 oneshot 所需的状态。

epoll 不为每种 fd 单独写轮询逻辑，而是调用文件对象的读写就绪判断。管道、socket、设备和 eventfd 各自定义什么叫“可读/可写”，epoll 层只负责事件掩码匹配和批量返回。epoll fd 可以被另一个 epoll 关注，用于嵌套关系和环路检查；当前 epoll 文件自身不通过普通 `read_ready` / `write_ready` 产生读写就绪事件。

水平触发模式下，只要 fd 保持就绪就会持续通知；边沿触发模式只在状态从未就绪变为就绪时通知一次；oneshot 条目触发后自动禁用，等待用户重新激活。epoll 嵌套时内核限制嵌套深度并检测环路，防止自引用导致无限遍历。

=== eventfd

eventfd 是一个以 fd 形式暴露的 64 位事件计数器。写入会累加计数，读取会取走当前值并清零；信号量模式下，每次读取只消耗 1。由于 eventfd 同样实现读写就绪判断，它可以被 epoll 监控，成为事件循环中的可编程唤醒源。写入后，等待循环下一次检查会看到该 fd 可读并返回事件；当前实现没有为 eventfd 单独维护 wait queue，而是复用 fd 事件等待轮询。
