= 总结和展望

== 已验证的功能

本阶段的验证围绕决赛环境中的真实使用链路展开。启动、内存和调度部分使用 `-m 8G -smp 8` 的 QEMU 配置检查多核启动、页表建立、动态物理内存管理和线程调度；文件系统、IPC 和系统调用部分通过定向回归检查错误返回、阻塞唤醒、引用计数和失败回滚；网络部分则检查 TCP/UDP loopback、批量消息、socket 关闭以及 `/proc/net/tcp{,6}` 状态快照。

=== 连续测例通过情况

RV64 和 LoongArch64 均完成四种 profile 的构建。定向 glibc 回归覆盖 78 项 clone、futex、mmap、共享内存、文件操作、定时器和 epoll 场景；高风险用例在 4G/8 vCPU 条件下重复运行时，没有出现内核 panic、TFAIL 或 TBROK。CAgent 的网络和系统调用路径也完成连续通过，说明动态加载、进程创建、文件访问、时间接口和 socket 可以在同一条用户程序链路中协同工作。

短回归用于定位具体语义，连续回归用于检查并发稳定性，双架构构建用于确认平台边界没有混入另一套实现。三类证据结合起来说明修改不是只对单个测例生效。

=== 用户程序运行能力

决赛阶段的用户态运行能力主要通过 CAgent 和 Rust toolchain/minibuild 体现。CAgent 可以完成动态 ELF 启动、glibc 初始化、文件创建与读写、目录遍历、时间查询以及本机 TCP/UDP 操作；这些程序依赖的 `clone`、`execve`、`futex`、`mmap`、`epoll` 和 socket 接口由对应内核模块共同提供。

在 8G/8 核 QEMU 工作负载中，Rust 工具链能够进入并行 crate 编译阶段，LoongArch 已完成一轮完整 BuildStorm 并生成正常产物。调度、futex、页表更新、ext4 写回和 exec 回滚等路径因此接受了比单个功能测例更长的组合压力。`/proc/f7ly/perf` 记录的 per-CPU 计数和系统调用耗时，则为这些运行结果提供了可重复的观测入口。

== AI 工具使用说明

本项目在 2026 年开发过程中使用了 AI Agent 辅助工程开发。主要工具为 VSCode / Codex 交互环境中的 Codex，相关大模型为 GPT-5。AI 工具的定位是工程协作者：用于阅读代码、定位模块、生成候选补丁、分析日志、整理连续测例结果、维护项目文档草稿。系统设计、需求取舍、最终代码合入、测例是否计入通过、提交记录与答辩材料由参赛队员负责。

为避免 AI 工具零散介入导致工程过程不可追踪，项目建立了一套固定协作流程：

#figure(
  text(size: 8.5pt)[
    #set par(justify: false)
    #table(
      columns: (1.2cm, 2.4cm, 1fr),
      align: (center, left, left),
      inset: (x: 3pt, y: 2.5pt),
      table.header([*阶段*], [*机制*], [*说明*]),
      [1], [*规则入口*], [所有 AI Agent 在行动前先读取 `AGENTS.md`，遵循沟通语言、Git 操作边界、Python 环境、日志保存、scoreboard 更新、危险操作禁令和人工验收规则。],
      [2], [*任务路由*], [根据任务类型读取 `agent_docs/` 下的配套文档：架构定位读取 `project_architecture.md`，构建调试读取 `development_debugging.md`，连续测例状态维护读取 `scoreboard.md` 与 `scoreboard/README.md`。],
      [3], [*计划记录*], [功能开发、性能优化、平台适配和测例修复先在 `plan_docs/` 中建立或更新任务计划，写清目标、阶段、验证点和风险；完成后只添加“已完成，待验收”标记。],
      [4], [*实现与验证*], [AI Agent 可以生成候选代码补丁、Typst 文档草稿和调试命令，但每次修改后必须执行语法检查、构建或窄验证；QEMU 长输出写入 `logs/` 或 `logs/run/`。],
      [5], [*人工验收*], [参赛队员检查 diff、验证日志和实际运行效果后决定是否接受修改；未经人工确认，AI Agent 不提交 commit、不推送远端、不改写历史。],
      [6], [*成果入账*], [若某个连续测例被验证通过，先在 scoreboard 对应架构、libc 和小分文件中写入 `PASS` 及简短依据，再由生成器刷新汇总。],
    )
  ],
  caption: [AI Agent 协作流程],
) <tab:ai-agent-workflow>

这套流程把 AI 工具的贡献分成三类可追踪成果：第一类是任务计划与交互摘要，保存在 `plan_docs/`；第二类是候选实现和文档修改，体现在 git diff 和后续 commit 记录中；第三类是测例验证结果，沉淀在 `logs/`、`logs/run/` 和 `scoreboard/`。因此，AI 工具参与的工作不会绕过人工 review，也不会直接替代参赛队员对正确性、可维护性和比赛诚信披露的责任。

== 总结与后续工作

本阶段的主要成果，是把内核从面向单核功能回归的实现，推进为能够承受多核、大内存、动态用户程序和 Linux ABI 访问的整体系统。启动信息、平台组合、设备后端、内存空间、进程线程、文件系统、IPC、系统调用和网络状态之间已经形成连续的责任链，用户态能够通过返回值、错误码和 `/proc` 文件观察到内核状态。

后续工作将沿现有边界继续推进：在固定宿主机条件下积累 RV/LoongArch 的多轮 BuildStorm 数据，持续观察长时间编译中的内存回收、页缓存和 ext4 写回行为；在不改变 socket 与 ONPS 公共接口的前提下扩展真实网卡收发、路由和 IPv6 状态；接入新板卡时复用 `BootInfo`、IRQ、block 和 net backend 边界，把平台差异限制在组合配置和设备驱动层。

总体而言，决赛阶段的优化不是孤立的测例修补，而是围绕架构承载能力、Linux 语义一致性和压力环境可复现性完成的一次系统性收口。
