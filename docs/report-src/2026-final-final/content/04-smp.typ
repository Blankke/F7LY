= 04 工作三：多核代码

== CPU 虚拟化与拓扑

决赛阶段的多核代码，不再把“核数增加”理解成单纯把 QEMU 参数从 1 改到 8，而是要让 CPU 拓扑、调度、页表、等待队列和中断路径都围绕真实可用的核心集合运行。CPU 虚拟化与拓扑这一节，先解决“系统到底看见哪些 CPU、这些 CPU 的状态如何表达、每个 CPU 维护哪些本地对象”三个问题。

#figure(
  image("fig/4.1_CPU虚拟化.png", width: 100%),
  caption: [CPU虚拟化],
) <fig:smp-cpu-virtualization>

=== DTB 识别 CPU / hart

CPU / hart 信息不再写死在通用代码里，而是统一从 DTB 读取。启动阶段根据固件提供的设备树解析出 CPU 节点、hart id、启动顺序和可能的次核集合，再把这些事实交给公共启动流程。这样，RISC-V QEMU、LoongArch QEMU 和实板画像都能沿同一条启动链路建立 CPU 拓扑，而不需要在通用代码中维护两套或多套固定核数假设。

这一做法的关键，是让 CPU 拓扑与平台画像分离。平台画像决定这台机器应该有哪些板级资源，DTB 决定这次启动实际看到了哪些 hart；如果 DTB 没有声明某个 CPU，内核就不应把它当成活跃核心。这样，后续调度、亲和性、`getcpu`、`/proc/cpuinfo` 和 `/sys/devices/system/cpu` 都能从同一份拓扑事实派生。

=== online / possible mask

为了让调度和用户态视图一致，F7LY-OS 将 CPU 集合拆成 possible 和 online 两类。possible mask 表示本次画像和 DTB 允许出现的 CPU 集合，online mask 表示当前已经完成启动、可以参与调度的 CPU 集合。调度器、affinity、`getcpu` 和 `/proc` / `/sys` 视图都只以 online CPU 为准。

这一层的作用，是把“启动时能看到多少 CPU”和“现在真正可以跑任务的 CPU”分开。次核可能已经在 possible 集合中，但尚未完成初始化、内存屏障或中断接入时，不应进入 online。这样可以避免用户程序读到一个已经声明却不能执行任务的假 CPU，也避免调度器提前把任务派发到尚未就绪的核心。

`/proc/cpuinfo`、`/sys/devices/system/cpu/online`、`sched_setaffinity` 和 `getcpu` 都建立在同一份 online mask 上。CAgent 的 cpu 项、BuildStorm 中的 Rust 编译进程以及后续多线程回归都依赖这个集合的稳定性：用户程序看到的 CPU 数量必须和调度器实际使用的 CPU 数量一致。

=== per-CPU PLIC 与本地状态

每个 CPU 都维护自己的本地中断和运行状态。中断控制器的 claim / complete、当前线程指针、irq_depth、timeslice 和本地调度状态都属于 per-CPU 对象，不应该放到全局单例里反复加锁。这样，CPU 处理本地中断和切换时，只需要访问自己的上下文，而不必扫描全局结构。

对于中断系统而言，per-CPU 状态的意义是把“中断从哪来、该由谁处理、处理到什么阶段”分开。外部中断到达时，先在当前 CPU 的 context 上 claim，再由对应 handler 处理，最后 complete；用户态看到的只是中断和调度结果，不需要知道 claim / complete 属于哪一个 CPU。对于多核调度而言，这种本地状态也减少了热路径竞争，避免每次中断都去修改全局数据。

=== per-CPU 内存分配

除了中断和调度状态，CPU 还应尽量保留自己的高频内存对象。页、对象和小块分配如果总是从全局分配器拿，会让共享分配器在 8 核下形成热点；因此决赛阶段更强调 per-CPU 内存分配和局部对象复用，把频繁申请 / 释放的页和临时对象尽量留在本核。

这种分配倾向并不意味着彻底拆开所有内存，而是把高频路径上的压力从全局资源上移走。高频页、临时调度结构、局部缓存和中断处理所需的小对象优先从本核拿，只有跨核共享对象、全局页缓存或需要一致性管理的页面才进入共享路径。这样，后续的 SMP 调度、TLB shootdown 和等待队列才能建立在更低的共享竞争上。

== 内存模型与屏障

#figure(
  image("fig/4.2_内存模型与内存屏障.png", width: 100%),
  caption: [内存模型与内存屏障],
) <fig:smp-memory-model>

=== 页表更新

无论是映射新页、撤销映射、修改权限还是处理 COW，内核都必须先完成页表写入，再让本地或远端 CPU 失效旧的 TLB 项。也就是说，PTE 的更新不是一个孤立动作，而是“修改页表 - 发布修改 - 发出 shootdown - 等待确认”的完整序列。

这里的关键约束，是不能让 CPU 继续使用已经失效的地址转换。对本地 CPU 而言，页表项写入后要配合必要的屏障和刷新；对其他 CPU 而言，则要通过 shootdown 通知它们丢弃旧缓存，避免某个核心仍然握着旧权限、旧物理页或旧映射继续执行。这样，`mmap`、`munmap`、`mprotect`、COW 写时复制和缺页修复才能在多核下保持一致。

F7LY-OS 因而把 TLB shootdown 视为地址空间协议的一部分，而不是“顺手发个 IPI”。只有当修改已经对发布方可见、远端确认已经收到并处理失效请求后，相关地址空间对象才允许进入下一步状态转换。

=== acquire / release 语义

在共享状态发布上，F7LY-OS 依赖 `eastl::atomic` 的 acquire / release 语义来约束跨核可见顺序。release 用在“先写数据、后发布状态”的路径上，例如任务状态、等待标志、引用计数归零通知、对象就绪位和页表相关标记；acquire 则用于“先看到状态、再读取伴随数据”的路径上，例如从 runnable 位图、等待队列或共享对象标记中读取后再访问其余字段。

这种语义的作用，不是让所有访问都变慢，而是把真正需要同步的点显式标出来。对调度、futex、pipe、eventfd、共享内存和 epoll 这类并发路径来说，最重要的是“状态变化和附带数据不能错序”。release 保证发布者写在前面的内容不会被重排到状态更新之后，acquire 保证读取者看到状态后能观察到与之匹配的数据。

因此，任务唤醒、对象激活、引用计数递减和跨核队列投递，都不是裸写一个 bool 就结束，而是按统一的原子语义完成。这样，多个 CPU 才能在没有全局大锁的情况下，对同一份共享对象形成一致的观察结果。

=== 缺页、COW 与并发地址空间修改

缺页、COW 和 mmap 并发修改是内存模型最容易暴露竞态的地方。一个进程可能在某个 CPU 上触发 page fault，另一个 CPU 同时在修改 VMA、释放 object 或撤销映射；如果没有锁和引用计数配合，就会出现“页已经回收，但 fault handler 还准备安装它”的问题。

F7LY-OS 的处理方式，是把 MM、VMA 和 VmObject 的生命周期拆开管理。页错误处理时先在受控锁域内确认地址空间仍然有效，再决定是分配新页、复制共享页，还是重新建立映射；而对 COW、`mmap` 和 `munmap` 的并发修改，则依赖对象锁、引用计数和必要的屏障，保证 fault path 读到的 VMA、object 和 PTE 仍然互相匹配。

这一层的目标是避免“看起来还能访问，实际上已经换对象”的竞态。只要 VMA、MM 和 page object 的发布与回收顺序稳定，page fault 就能在多核下以可重复的方式完成修复，而不会因为并发修改出现随机崩溃或错误映射。

=== 跨核延迟回收

页表、ASID、MM、trapframe 和 kstack 都可能被某个 CPU 暂时持有，不能在本地状态刚改完就立刻释放。对这些对象的回收必须等到远端确认已完成失效、引用计数归零或相关 CPU 退出临界区之后，才能真正释放。

这意味着，销毁一个地址空间不是单次 free，而是先撤销可见性，再等待使用者退出，再做最终回收。对 ASID 和页表页来说，要等 shootdown ack；对 MM、trapframe 和内核栈来说，要等最后一个持有者离开；对跨核共享对象来说，要等引用计数归零并确认没有后续访问者。只有这样，旧状态才不会在另一个 CPU 的缓存里继续“活着”。

SMP 调度负责把任务放到正确的核上，内存模型与屏障负责保证这些核看到的是同一份状态。前者决定“谁去跑”，后者决定“跑之前看到什么”。

== 等待队列与跨核唤醒

等待队列解决的不是“线程怎么睡”，而是“线程睡下去以后，谁在什么时机把它准确地叫醒”。F7LY-OS 把等待、唤醒、超时和跨核通知放在同一条语义链路里处理，避免把不同对象的阻塞逻辑散落到各处，最后形成难以验证的隐式唤醒路径。

#figure(
  image("fig/4.3_等待队列与跨核唤醒.png", width: 100%),
  caption: [等待队列与跨核唤醒],
) <fig:smp-wait-queue>

=== 跨核唤醒

当目标线程不在当前 CPU 上，或者它所在的 CPU 已经进入 idle，单纯修改本地状态并不足以让它尽快恢复运行。此时需要通过 `kick_cpu` / IPI 把“有任务可跑”这件事显式通知给远端 CPU，让它尽快从空闲态回到调度检查点。

F7LY-OS 的跨核唤醒遵循固定顺序：先把任务放回等待对象对应的 runnable 路径，再更新目标 CPU 的本地可见状态，最后发出 IPI。这样远端 CPU 收到中断后，只需要检查本地 runnable 位图和相关唤醒标记，就能知道自己是否需要立即切换，而不必再次扫描全局进程表。

这条路径对于 futex、pipe 读写、epoll 事件、timer 到期和进程退出都很关键。一个核上睡眠的线程，经常是被另一个核上的 I/O 完成、计时到期或锁释放唤醒。跨核唤醒把“事件已经发生”与“目标线程已经可运行”之间的空隙收紧，减少用户态看到的额外阻塞。

=== 超时等待优化

等待并不总是依赖外部事件，很多场景还要处理超时。F7LY-OS 不再采用每个 tick 广播式地扫描所有等待者，而是把超时检查收束到统一时间路径中，由时钟到点后只处理真正到期的对象和线程。

这样做的目的很直接：把“没有到期的等待”排除在热路径之外。无论是 `nanosleep`、futex 超时、epoll wait 还是定时器相关等待，都只在统一的时间检查点判断是否到期，而不是每个 tick 都去唤醒所有候选线程。对 8 核和更高并发场景来说，这样可以明显减少无效遍历和跨核干扰。

超时等待的另一层收益，是让时间语义更集中。等待对象到期时，统一时间路径负责把它从等待队列转回 runnable，并触发必要的跨核唤醒；调用方只关心“是否超时返回”，不需要知道底层是哪个 tick、哪个 CPU 或哪条队列做了判断。

=== 分桶等待队列

为了避免所有睡眠对象挤在同一个锁或同一条链表上，F7LY-OS 将等待队列按对象 key 分桶。`futex` key、具体等待对象、共享资源和定时相关条目都会进入对应 bucket，唤醒时只扫描相关桶中的等待者，而不是遍历整个系统的睡眠线程集合。

分桶的核心价值，是把等待对象的局部性保留下来。一个 futex 只应该影响持有同一 key 的线程，一个 pipe 只应该影响这根管道上的读写者，一个事件对象也只应该唤醒自己的等待列表。bucket 化之后，唤醒操作从“全局找人”变成“在局部桶里找相关者”，减少锁竞争，也减少误扫无关线程带来的缓存抖动。

bucket 负责限定“该看哪一组等待者”，per-CPU runnable 负责限定“该由哪一个 CPU 去跑”，两者共同把阻塞/唤醒的开销压缩到最小范围。

=== sleep / wakeup 顺序

线程入睡、锁释放、状态切换和唤醒入队这几个动作如果顺序不稳，就会出现丢唤醒、双重入队或者“线程还没睡下去就被认为睡着了”的问题。F7LY-OS 采用固定的 sleep / wakeup 顺序，把这些步骤串成严格的协议。

线程进入睡眠前，先把自己挂入等待对象和对应 bucket，再释放相关锁，随后切换到阻塞态。唤醒时则相反：先更新线程状态，再把它从等待对象中移到 runnable 路径，最后根据目标 CPU 是否本地可达决定是否发出 IPI。这样可以保证睡眠对象和运行队列之间始终存在明确的过渡点，不会因为并发而出现状态撕裂。

这个顺序对 futex、pipe close、epoll 事件和 timer 到期尤其重要。它确认清晰的先后关系，避免由于锁释放过早或状态更新过晚而丢掉一次真实唤醒。

等待队列与跨核唤醒的最终目标，是让阻塞路径可预测。线程睡下去时能准确进入对应桶，事件到来时能准确找到它，远端 CPU 能在正确时机被 kick，超时路径也能沿统一时间链路返回。这样，等待语义就不会变成一堆彼此独立的补丁，而是调度体系里自然的一环。

== 锁拆分与死锁规避

F7LY-OS 按对象边界、按等待范围、按 CPU 局部性拆分锁域，让热点路径尽量只接触局部对象，不再围绕一个全局锁反复争用。

#figure(
  image("fig/4.4_锁拆分与死锁规避.png", width: 100%),
  caption: [锁拆分与死锁规避],
) <fig:smp-lock-split>

=== 从单一大锁到对象锁

内核早期可以依赖少量全局锁维持正确性，但在多核和高并发下，这种方式会把文件系统、进程管理、内存管理和 I/O 路径全部串成一条线。只要某个子系统占着大锁，其他核就只能等待，哪怕它们访问的是完全无关的对象。于是，系统的瓶颈不再是计算，而是锁本身。

F7LY-OS 采用的策略，是把“大锁保护整个内核状态”改成“对象锁保护对象生命周期”。文件、进程、地址空间、等待对象和设备状态都尽量拥有自己的锁，热点路径只锁住当前真正要修改的那份数据。这样，多个 CPU 可以并行处理不同文件、不同进程或不同等待桶，而不会在一个中心锁上排队。

这种拆分不让锁的作用域和数据结构的作用域一致。谁负责维护某个对象，就由谁的锁保护它；谁不需要修改它，就不必进入同一锁域。

=== 文件系统锁拆分

文件系统同时涉及目录、元数据、文件对象、缓存和管道缓冲，F7LY-OS 在这里保留必要的元数据锁，同时拆出 `file lock`、`cache lock`、`fd/object lock` 和 `pipe lock`，避免把读写、查找、引用和缓冲管理混在一起。

元数据锁只负责目录项、inode 关系和必须串行化的结构更新；`file lock` 负责单个文件对象的读写状态；`fd/object lock` 负责描述符与底层对象的绑定；`pipe lock` 则专门保护管道缓冲区和读写指针。这样，文件系统内部的多个面可以独立运行，`read`、`write`、`dup`、`close`、`rename` 和 `pipe` 的路径不会因为一把大锁互相阻塞。

当锁粒度缩小后，缓存行争用也会下降。对高并发文件访问来说，真正热的往往不是整个文件系统，而是某个 inode、某个 pipe 或某个文件描述符；把锁留在对象附近，竞争就会自然收敛到局部范围。

=== 进程、futex 与内存锁拆分

F7LY-OS 将 PCB lock、wait lock、futex bucket、mm lock、VMA lock 和 object lock 分离，让进程状态、等待关系、虚拟内存布局和底层共享对象各自有清晰的保护边界。

PCB lock 保护进程/线程控制块本身，包括状态转移、调度相关字段和必要的句柄引用；wait lock 只保护等待队列和睡眠/唤醒关系；futex bucket 负责按 key 管理局部等待者；mm lock 约束整个地址空间的大范围修改；VMA lock 只覆盖单个虚拟内存区域；object lock 则保护共享页、匿名对象或文件映射背后的实体生命周期。

这样拆开以后，page fault、`mmap`、`munmap`、futex wait/wake、线程退出和进程回收就不必共享一把总锁。一个线程在某个 VMA 上缺页，不会挡住另一个线程对 futex 桶的唤醒；一个对象的引用计数变化，也不会把整个进程表锁住。锁的职责越清楚，路径之间的耦合就越低。

=== bucket 按编号加锁

在等待队列和 futex 这类分桶结构里，死锁风险经常来自“同时碰多个桶”。F7LY-OS 采用按编号顺序加锁的策略：需要访问多个 bucket 时，必须按照统一的编号顺序进入，不能临时按业务顺序乱拿。这样就把“谁先锁谁后锁”变成确定规则，避免两个 CPU 以相反顺序拿锁而互相等待。

编号顺序的价值，不只是防止死锁，还能让复杂操作的行为稳定下来。比如某些跨桶唤醒、合并等待者或回收条目动作，只要遵守统一顺序，就不会因为不同调用路径的锁顺序不一致而形成环路。对多核系统而言，显式的全局顺序比“靠约定小心一点”更可靠。

同样的原则也适用于对象回收。涉及多个桶或多个对象时，先确认顺序，再进入临界区，最后统一释放。这样，等待队列、futex 和跨核唤醒就能在局部加锁的前提下保持可证明的无环关系。

=== pipe 与缺页的锁边界

某些路径并不适合在锁内等待慢操作。pipe 是典型例子：管道锁只应保护缓冲区和读写指针，不应把可能触发缺页的访问留在锁内，否则一个页错误就可能让整个 pipe 进入不可预测的阻塞状态。F7LY-OS 因而要求在持有 pipe 锁时避免缺页，必要的内存访问应当提前准备好，或者在进入临界区之前完成。

这个原则的目的，是防止“持锁睡眠”把局部锁扩散成系统级阻塞。对管道、文件缓存和共享缓冲区而言，锁只负责短时间的状态修改；凡是可能涉及页分配、COW 或远端回收的步骤，都应放到锁外处理，或者先确保对象已稳定存在。这样既减少锁持有时间，也避免不同子系统在锁内嵌套等待。

=== shootdown 完成后再释放跨核对象

跨核对象的释放要格外小心，因为它们往往已经被别的 CPU 观察过。F7LY-OS 规定，涉及页表页、ASID、MM、trapframe、kstack 或其他被远端 CPU 看到的对象时，必须等 shootdown 或引用计数确认完成后才能回收，不能只在本地状态更新后就直接 free。

只要远端 CPU 还可能持有旧映射、旧上下文或旧指针，释放就必须延后；确认所有相关 CPU 都已经收到失效通知并退出临界区之后，才能真正销毁对象。这样可以避免一边回收、一边仍被另一个核访问的悬空引用问题。

F7LY-OS 的死锁规避是一组统一的约束：锁域尽量小，顺序必须固定，慢操作不能卡在锁内，跨核对象必须等待失效确认后再回收。这样，文件系统、进程管理、内存管理和调度路径才能在多核下同时保持正确性和可伸缩性。
