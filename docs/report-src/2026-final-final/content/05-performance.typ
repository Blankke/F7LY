= 工作四：性能优化

== 块设备驱动与 I/O 调度
#figure(
  image("fig/块设备驱动与IO调度.png", width: 100%),
  caption: [块设备驱动与IO调度],
) <fig:performance-block-io-scheduler>

=== 统一块设备接口

上层文件系统不应该关心设备是 virtio-blk、平台本地盘，还是其他后端，只应该看到统一的 `BlockDevice::read_blocks` 和 `BlockDevice::write_blocks` 接口。这样，ext4、缓存层和回收逻辑都围绕同一套块读写契约工作，平台差异被收敛到 block backend 里，而不是扩散到各个调用点。

这种抽象的意义，在于把“怎么读写”与“从哪里读写”分离开。上层只提供逻辑块号、长度和缓冲区，下层负责把这些请求翻译成设备相关的扇区访问、DMA 描述符和完成中断。这样一来，文件系统路径、启动盘路径和数据盘路径就可以共享同一套块层实现，而不用为不同硬件复制逻辑。

对性能而言，统一接口还有一个直接收益：缓存、预读、回写和提交合并都可以在块层内部完成，不必让每个文件系统上层自己拼装 I/O。接口收敛后，后续优化只要改 block backend 和调度器，就能影响所有使用者。

=== 128 深度 VirtQueue

块层的并发能力依赖队列深度。F7LY-OS 使用较深的 VirtQueue，让 descriptor chain 能够保持多请求 in-flight，而不是每提交一次就等待一次完成。只要还有空闲描述符，驱动就继续 dispatch；只要队列还没有填满，就不人为把吞吐量卡在单个请求的往返上。

我们给设备侧、文件系统侧和内核页缓存之间留出足够的流水线空间。写回、预读和同步提交可以同时推进，短请求不会因为队列太浅而频繁阻塞，顺序吞吐也能借助多个在途请求维持稳定。

VirtQueue 在这里承担的是“把同步 I/O 变成可并发流水”的角色。驱动从上层拿到多个块请求后，尽量把它们组织成连续的 descriptor chain，再一次性送入队列；设备完成后再统一回收描述符。这样，提交端不会被完成端反复打断，完成端也不会因为队列太短而限制设备利用率。

=== mClock 软件调度

有了足够深的队列，还需要调度策略决定“先发谁、后发谁”。F7LY-OS 在块层引入 mClock 风格的软件调度，把请求按 8 个 service class 管理，并通过 R/W/L 标签区分不同类型的 I/O 压力。这样，设备资源不会被某一种请求长期占满，读、写和低延迟控制类请求也能保持相对平衡。

class 内部再使用 per-flow RR，可以避免单个进程或单条流量把队列独占。换句话说，mClock 负责决定类之间的份额，per-flow RR 负责决定类内的公平性。这样，短小的元数据访问、持续写回和批量顺序读都能在同一块设备上共存，不会因为某个大流量进程而把其他进程完全饿死。

这一层调度的目标是把“吞吐”和“响应”拆开处理。块设备既要能持续跑满，也要让关键请求不被长期淹没。mClock 提供的就是这种可调的平衡点。

=== 完成与唤醒

块设备请求的生命周期不能停留在“已经发出去”，还要明确落到“已经完成并唤醒等待者”。F7LY-OS 的完成链路采用 `used ring -> completed -> wakeup` 的顺序：设备把请求放入 used ring，驱动把它转入 completed 状态，再由完成路径唤醒等待该 I/O 的线程或上层回调。

上层真正关心的是“等待这次 I/O 的线程是否可以继续运行”。只有在完成状态与等待队列同步更新之后，`read`、`write`、回写和同步刷盘的调用方才会解除阻塞。完成路径如果只回收 descriptor、不唤醒等待者，就会让上层看到假性挂起；如果先唤醒再更新状态，又可能造成重复提交或状态错乱。

因此，F7LY-OS 把 descriptor 回收和等待唤醒放在同一条完成路径里处理：先确认请求完成，再回收描述符，最后让相关线程重新进入 runnable。这样，I/O 完成不只是“设备侧结束”，而是“系统侧可以继续推进”的明确边界。

=== 连续块提交

为了减少小块请求的累计成本，F7LY-OS 在块层尽量做连续块提交。lwext4 以 4KiB block 为基本单位，但驱动层可以把多个连续请求合并到更大的 bounce buffer 中，例如 128KiB 级别的连续区间，然后一次性提交为单次 sector transfer。

这样做的好处是把上层碎片化的块访问转成更接近硬件顺序的传输。文件系统可能按页、按 inode 或按元数据碎片地产生请求，但驱动层可以把相邻块合并，减少 descriptor 数量、减少队列切换，也减少设备侧的寻址和调度开销。对顺序读写、日志提交和元数据批量回写来说，这种合并通常能直接换来更稳定的吞吐。

连续块提交并不改变上层语义。lwext4 仍然以 4KiB 为自己的逻辑边界，文件系统看到的是正常的块接口；只是块层把多个相邻逻辑块打包成更大的物理传输，以更少的提交换更高的利用率。这样，统一接口、队列深度、软件调度和完成唤醒就连成了一条完整的性能链路。

== Ext4 与文件页缓存

ext4 这一层的性能优化让文件页缓存、块回写和文件映射共享同一套缓存语义。F7LY-OS 在这一层的核心目标，是让 clean page 始终可复用、让写入尽量私有化、让回收和预读围绕局部性展开，从而把文件系统路径变成稳定的缓存命中路径。

#figure(
  image("fig/F7LY_6.2_Ext4与文件页缓存.png", width: 100%),
  caption: [Ext4与文件页缓存],
) <fig:performance-ext4-page-cache>

=== 缓存 clean page

文件页缓存的基本原则，是把已经从磁盘读入、尚未被修改的页面作为 clean page 保留在全局缓存中。这样的页面既可以被 `read` 直接复用，也可以被 `mmap`、`exec` 和其他只读路径共享，不需要每次访问都重新回盘。只要页面保持只读状态，它就天然适合在多个用户和多个映射之间复用。

F7LY-OS 在这里强调的是“缓存页是共享资源，但写路径不能直接污染它”。对于 `MAP_PRIVATE` 写入，页面必须先私有化，再进行实际修改，避免把共享 cache 页原地改脏。这样做的结果，是只读文件映射、执行映射和普通读取都可以继续复用同一份 clean page，而不会因为一次私有写访问破坏全局缓存的稳定性。


=== 回收与预读

页缓存不能无限增长，因此 F7LY-OS 把回收和预读放到同一套局部策略里处理。每个 shard 维护自己的独立 LRU，需要内存时可以主动 reclaim clean pages；而当访问模式表现出顺序 miss 时，系统会触发 16 页预读，把后续很可能要用到的内容提前拉进缓存。

独立 LRU 的作用，是把不同文件热点的回收压力拆开，避免全局一个队列里所有文件互相干扰。这样，一个高频目录或大文件流式读取的回收，不会直接影响到另一组小文件和元数据页。回收对象优先选择 clean page，因为它们可以直接丢弃或重新加载，回收成本最低，也最适合在内存紧张时先让出空间。

预读则是另一端的优化。文件访问一旦出现连续 miss，就说明后续页面大概率也会被访问；此时一次预读 16 页，比逐页按需回填更能减少 I/O 往返和调度碎片。对顺序读、`exec` 装载和某些线性遍历场景来说，这种预读能够显著压低后续缺页和块请求的峰值。

=== 锁粒度

F7LY-OS 在这一层采用读锁加 `bcache` 独立锁的组合，把文件读取路径和块缓存管理路径分开，避免一个统一大锁同时覆盖元数据、缓存页、回写和块提交。

读锁主要保护可共享访问的文件数据和缓存引用；`bcache` 锁则负责块缓存内部的索引、插入和回收。这样，多个只读操作可以并发命中缓存，而不会因为缓存维护把所有文件访问串行化。只有确实需要修改缓存结构或回收条目时，才进入较重的锁域。

这种粒度拆分把“访问文件内容”和“维护缓存结构”解耦。上层读路径只要拿到合适的缓存页，就可以快速完成数据复制；底层维护逻辑则在更小的临界区里处理 LRU、回写和缓存一致性，尽量不阻塞前台请求。

=== 复用路径

ext4 的性能提升，最终体现在路径复用上。`read`、`mmap` 和 `exec` 不应该各自维护一套互不相通的文件加载逻辑，而应尽量复用同一套页缓存和块读取路径。这样，磁盘数据只需要被读入一次，就可以在不同访问模式之间共享。

`read` 直接从页缓存中复制数据；`mmap` 把同一份 clean page 映射到用户地址空间；`exec` 则借助页缓存读取 ELF 内容，再把代码段和只读数据段挂到同样的缓存体系上。三条路径共享同一份缓存后，不仅减少重复 I/O，也减少不同机制之间的语义偏差。

== Pipe：连续缓冲与等待唤醒

pipe 的优化把内部缓冲、容量管理和等待通知做得更顺。F7LY-OS 在这一层保持 Linux 兼容的 pipe 语义不变，只把搬运方式从零散逐字节转向连续 chunk，把状态判断从隐式轮询转成明确的可读/可写边界。

#figure(
  image("fig/F7LY_6.3_Pipe连续搬运与唤醒.png", width: 100%),
  caption: [Pipe连续搬运与唤醒],
) <fig:performance-pipe-buffer-wakeup>

=== 数据搬运：从逐字节到连续 chunk

pipe 的 ring buffer 语义仍然保留，但真正的搬运不再围绕单字节反复取模。缓冲区只在 head / tail 回绕处才切分成两个连续区间；在大多数情况下，当前可读或可写的数据都是一段连续 chunk，可以直接用一次 `memmove` 完成搬运。

这样让 user buffer 和 pipe buffer 之间尽量形成连续拷贝，而不是在每个字节上都做边界检查。对于读路径，可以直接把当前连续可读区间拷到用户缓冲；对于写路径，也可以直接把连续可写区间从用户数据写入 pipe 缓冲。中间少了多次分段处理和取模运算，CPU 开销和缓存抖动都会更低。

这类优化不改变 pipe 是字节流的本质，只是把内部实现从“每次处理一个小片段”变成“优先处理整段连续区间”。在高并发和大块传输场景下，这种变化会直接体现在更少的函数调用、更少的边界分支和更稳定的吞吐上。

=== 容量策略

容量策略必须兼顾兼容性和性能。F7LY-OS 保持 Linux 兼容的默认 4KiB pipe 容量，同时支持 `F_SETPIPE_SZ` 动态调整到更大的容量，最大可到 64KiB。也就是说，性能优化不是靠改变默认行为，而是靠在合法扩容后提供更好的连续缓冲和更少的唤醒抖动。

默认 4KiB 让短命进程、普通 shell 管道和测试程序都能保持和 Linux 一致的预期；而更大的容量则服务于需要更高吞吐的生产路径。容量变大后，pipe 可以容纳更多连续数据，降低频繁睡眠和频繁唤醒的概率，也减少读写两端在小窗口里反复争用锁和缓存。

这种策略本质上是把“语义固定”与“性能可调”分开。默认大小不动，兼容性就稳；动态扩容按需开启，吞吐就能上去。F7LY-OS 不靠破坏默认语义换性能，而是通过可控参数给重负载场景留出空间。

=== poll / epoll 状态

pipe 的可读 / 可写状态必须能被 `poll` 和 `epoll` 准确观察到，所以状态更新不能滞后于数据变化。F7LY-OS 直接根据 count 和容量判断阈值：当前缓存里是否有足够数据可读、是否还有足够空间可写，都由这两个量即时得出，而不是额外维护一套容易失真的中间状态。

写入、读出和 close 都要同步更新等待队列和异步通知。数据写进去之后，如果从不可读跨到可读，就要唤醒等待读端；数据读走之后，如果从不可写跨到可写，就要唤醒等待写端；`close` 发生时，则要把等待对象和事件状态一起更新，避免等待者继续盯着一个已经失效的 pipe。

因此， `poll` / `epoll` 看到的就是当前真实状态，而不是某个还没刷新的缓存副本。这样，事件通知可以和 pipe 内部的缓冲变化保持一致，既减少误唤醒，也减少漏唤醒。对 shell 管道、日志转发、脚本流水线和 `cat | grep | ...` 这类常见场景来说，这种一致性比单纯提升极限吞吐更重要。

所以我们希望把“连续数据搬运”和“等待状态同步”放在一起处理：一边减少内部碎片，一边保证可读可写状态随数据变化即时传播。这样，pipe 才能既保持 Linux 语义，又在高频数据流里表现得更稳定。

== VMA：索引、合并与文件映射

F7LY-OS 在这一层把 `syscall -> VMASpace -> Maple Tree -> VmObject` 作为主链路，让地址空间的查找、扩展和回收都落在可索引、可合并、可复用的结构上。

#figure(
  image("fig/F7LY_6.4_VMA索引与一致性.png", width: 100%),
  caption: [VMA索引与一致性],
) <fig:performance-vma-index-consistency>

=== 主链路

用户态发起 `mmap`、`munmap`、`mremap`、`mprotect` 或文件映射相关操作时，入口不会直接操作零散的 VMA 链表，而是先进入 `VMASpace`，再通过 Maple Tree 完成区间索引，最后落到具体的 `VmObject`。这样，系统调用层看到的是“我要改哪段地址空间”，索引层负责定位范围，实体层负责维护背后的对象生命周期。

这种分层的价值，在于把“查找”和“修改”拆开。地址空间里可能有大量小 VMA，也可能有少量大映射；如果只靠线性遍历，`mmap`、缺页和范围查询都会被拉长。Maple Tree 作为区间索引，可以把查找、插入和邻接区间管理做得更稳定，而 `VmObject` 则只负责实际承载匿名页、文件映射页或私有副本。

因此，VMA 不再只是一个地址区间描述，而是地址空间索引结构中的一个节点。这样，系统调用入口、缺页处理和回收路径就能通过同一套空间索引定位目标，不必在多份链表和多份对象表之间反复跳转。

=== 相邻匿名 VMA 合并

F7LY-OS 在这里支持相邻匿名 VMA 合并，把边界连续、权限一致、对象关系兼容的区间尽量拼接成更大的连续段。可以减少 VMA 数量，降低索引压力，也让后续的 `mprotect`、`munmap` 和 `mremap` 更容易在较少节点上完成。

合并由索引承担，而不是由调用方手工拼接。也就是说，申请新匿名区间时，系统会先看前后是否存在可以合并的空洞或同类区间，再决定是新建节点还是扩展已有节点。这样可以把大量短小的匿名映射收束为更少的连续区段，避免 8 GiB 级别大映射场景下出现过多零碎条目。

VMA 数量越少，地址空间查询越快，页错误路径越短，元数据维护也越轻。对于频繁创建、销毁、移动映射的程序来说，合并还能减少索引更新次数，让地址空间维护成本更接近连续区间的成本。

=== 文件映射进入 clean page cache

文件映射和页缓存之间不能是两套割裂的系统。我们的策略是，文件映射优先进入 clean page cache，先共享已有的干净页面；只有在写访问发生时，才通过 COW 或 private overlay 把私有副本分离出来。

通过让 `mmap` 和 `read` 共享同一份文件数据，只读文件映射可以直接复用缓存页，不必为每个映射再单独读盘；写访问则只在需要时私有化，避免把共享 cache 页原地改脏。这样，文件内容的共享性和写时隔离就能同时成立。

对性能而言，这意味着文件映射不再是一条完全独立的加载路径，而是页缓存体系的一个入口。文件被装入一次后，可以同时服务 `read`、`mmap` 和 `exec`；只有真正的私有写入才会把页面从共享缓存中拆开。这样既减少 I/O，又让缓存命中更加稳定。

=== 8 GiB 大映射与 64 位长度/偏移

大映射场景下，地址空间的长度和文件偏移都不能再靠 32 位思路处理。F7LY-OS 在这一层明确使用 64 位长度和偏移来支持 8 GiB 级别的大映射、跨页偏移和大文件访问，避免在长地址区间上出现截断或溢出。

这一点对 `mmap`、`mremap`、`munmap` 和文件偏移相关操作都很重要。映射长度必须能覆盖大块连续空间，偏移也必须能准确指向文件内部的任意位置；否则，用户程序在处理大文件、数据库段或大块共享映射时，很容易碰到错误对齐或地址回绕。

因此，VMA 层在这里不只是维护“有哪些区间”，还要确保“这些区间能覆盖多大的范围、文件数据能从哪一个偏移开始映射”。这也是为什么 64 位长度和偏移是这一节的基础，不是额外能力。

=== 缺页、PTE、Cache、TLB 一致性

我们把缺页、PTE、Cache 和 TLB 作为一条完整链路来看。用户访问触发 page fault 后，系统要根据 VMA 找到对应的 `VmObject`，再决定是从文件页缓存直接取页，还是分配匿名页、执行 COW 或重新建立映射；更新完成后，还要确保 PTE、缓存状态和 TLB 观察到的是同一份结果。

